# Walapp 컨셉 전환 설계서 — 「매일 뉴스 브리핑」 → 「지원금·제도 도감」

> 작성일: 2026-08-12
> 상태: **단계 1~5 구현 완료** (목 데이터 기준) · 단계 0(API 활용신청)·6·7 대기
> 선행 문서: `HANDOFF.md` (구 뉴스 브리핑 구조)
> 구현 결과와 실행 방법: `docs/IMPLEMENTATION.md`

---

## 1. 왜 바꾸는가 — 문제 진단

### 1.1 측정된 현상

| 지표 | 값 |
|------|-----|
| 총 포스트 | 272개 |
| 유니크 제목 | 230개 |
| **완전 중복 제목 비율** | **15.4%** (42개) |
| 최다 중복 | `"일자리전담반 개최"` 6회, `"아동양육시설 아동의 ADHD 치료"` 6회 |
| 기타 | `"청년 문화예술패스 발급"` 4회, `"청년미래적금"`·`"청년일자리도약장려금"` 각 3회 |

제목이 완전히 같은 것만 15%다. **의미 중복은 훨씬 높다.** 예를 들어 8/10·8/11 이틀 연속으로 `youth` 카테고리가 청년 문화예술패스를 다뤘고, `policy`와 `youth`는 애초에 같은 `korea.kr` 피드를 키워드로만 갈라 쓰기 때문에 소재 풀이 겹친다.

### 1.2 근본 원인 (코드 레벨)

세 지점이 맞물려 중복을 **구조적으로 보장**한다.

1. **소재를 "최신순"으로 뽑는다**
   `scripts/collect/korea_policy.py` — RSS/Google News를 최신순으로 받아 상위 N건을 후보로 삼는다. 뉴스 사이클은 같은 이슈를 며칠씩 반복 노출하므로, 최신순 선택은 필연적으로 어제와 겹친다.

2. **중복 제거가 "그날 수집분 안"에서만 동작한다**
   `base.py:dedupe_by_title()` / `korea_policy.py:_collect()` — 리스트 하나 안에서만 제목 정규화 비교를 한다. **어제 발행한 글의 존재를 모른다.**

3. **생성 단계에 발행 이력이 전혀 들어가지 않는다**
   `generate_post.py:_build_prompt()` — 프롬프트에 들어가는 건 `primary` 1건과 `supplementary` 2건이 전부다. LLM은 매일 백지에서 시작한다. 과거 272개 글의 존재를 알 방법이 없다.

추가로 `run_all.py:127`은 `max(items, key=len(content))` — 본문이 가장 긴 항목을 고른다. 본문 길이는 날마다 크게 안 변하므로 **같은 기사가 며칠 연속 1등**이 되기 쉽다.

### 1.3 포맷 자체의 한계

코드를 고쳐도 남는 문제가 있다.

- **뉴스는 소재 풀이 무한하지 않다.** 반복하는 풀이다. 중복 제거를 강화할수록 "쓸 게 없는 날"이 늘어난다.
- **1차 취재 없는 뉴스 요약은 SEO 가치가 사실상 0이다.** 원문(korea.kr, 연합뉴스)이 항상 상위에 있고, 요약본이 이길 방법이 없다.
- **수명이 48시간이다.** 272개 글이 쌓였지만 자산으로 축적되지 않는다.
- **환각 위험이 크다.** `2026-08-12-curious.md`의 제목 *"빛의 속도를 능가하는 에너지 발견"* 은 물리적으로 성립하지 않는 문장이다. 자유 서술형 뉴스 요약에서는 이런 사고를 막을 구조적 장치가 없다.

---

## 2. 새 컨셉 정의

### 2.1 한 줄 정의

> **대한민국의 모든 정부·지자체 지원 제도를, 제도 하나당 페이지 하나로 정리한 사전(백과).**

뉴스가 아니라 **레퍼런스 사이트**다. 발행 단위가 "오늘"에서 "제도"로 바뀐다.

### 2.2 전환의 핵심 — 글의 정체성(primary key) 변경

| | 현행 | 전환 후 |
|---|------|---------|
| 글의 키 | `날짜 + 카테고리` (`2026-08-12-youth.md`) | `제도 ID` (`youth-monthly-rent-support`) |
| 같은 주제 재등장 시 | **새 글 생성** → 중복 | **기존 글 갱신** → 중복 불가 |
| 소재 선택 | RSS 최신순 | 사전 구축된 큐에서 우선순위순 |
| 수명 | 48시간 | 영구 (제도 폐지 시까지) |
| 발행량의 상한 | 없음 (매일 4개 강제) | 큐 소진 시 발행 없음 (억지 생성 금지) |

**중복이 불가능한 이유는 규칙이 아니라 자료구조다.** 같은 제도는 같은 ID를 갖고, 같은 ID는 파일 경로가 같으므로, 두 번째 시도는 새 글이 아니라 덮어쓰기(=갱신)가 된다.

### 2.3 왜 이 주제인가

- **주제 우주가 유한하면서 충분히 크다** — 보조금24 기준 수천 건, 지자체 복지서비스까지 포함하면 1만 건 이상. 하루 5건씩 써도 수년치.
- **에버그린 + 갱신 수요 동시 충족** — 제도는 매년 금액·요건이 바뀐다. 갱신할 때마다 `dateModified`가 갱신돼 검색엔진이 신선도를 인정한다. 뉴스처럼 죽지 않으면서 뉴스처럼 신선하다.
- **검색 의도가 명확하고 상업적이다** — "청년월세지원 조건", "근로장려금 신청방법" 류의 고전환 롱테일. 광고 단가가 뉴스 대비 크게 높다.
- **AI에게 시키기 가장 안전한 작업이다** — 이미 구조화된 API 필드를 "쉬운 말로 풀어쓰기"만 시킨다. 사실·수치는 원본을 그대로 렌더한다. §6.3 참고.
- **기존 자산 재활용률이 높다** — `korea_policy.py` 수집 골격, 카드 UI, 다크모드, 검색, 카테고리 시스템, 배포 워크플로우 대부분 유지된다.

### 2.4 사이트 정체성

- **도메인·레포명 유지** (`adk24211.github.io/walapp`) — 변경 비용 대비 실익 없음
- `_config.yml`의 `title`/`description`은 교체
  - `title`: `지원금 도감` (안)
  - `description`: `정부·지자체 지원금과 복지 제도를, 받을 수 있는지부터 신청 방법까지 하나씩 정리합니다.`
- **필수 고지 강화**: "본 사이트는 정부 공식 사이트가 아니며, 신청은 반드시 공식 창구를 이용하십시오." — 지원금 정보 사이트는 정부 사칭 오해 소지가 있으므로 헤더·푸터·각 제도 페이지 3곳에 노출한다.

---

## 3. 데이터 소스

### 3.1 확인된 소스 (2026-08-12 기준 존재 확인)

| 소스 | 공공데이터포털 ID | 성격 | 역할 |
|------|------------------|------|------|
| 행정안전부_대한민국 공공서비스(혜택) 정보 (보조금24) | `15113968` | Open API | **주 소스.** 중앙부처·지자체·공공기관·교육청 수혜서비스 목록 + 상세 |
| 한국사회보장정보원_중앙부처복지서비스 | `15090532` | Open API | 복지로 기반 중앙부처 복지사업 |
| 한국사회보장정보원_지자체복지서비스 | `15108347` | Open API | 지자체 복지서비스 (지역 롱테일의 핵심) |
| 한국고용정보원_온통청년_청년정책 | `15143273` | Open API | 청년정책 전용 |
| 한국사회보장정보원_복지서비스정보 | `15083323` | 파일(CSV) | **폴백.** API 승인 지연 시 초기 시드용 |
| 기업마당 지원사업정보 | `bizinfo.go.kr/apiDetail.do?id=bizinfoApi` | Open API | (선택) 소상공인·기업 지원 확장 시 |

### 3.2 구현 전 반드시 확인해야 할 것 ⚠️

이 환경의 이그레스 프록시가 `data.go.kr`·`gov.kr`을 차단해 **응답 필드명을 확정하지 못했다.** 아래는 사람이 직접 해야 하는 선행 작업이다.

1. 공공데이터포털에서 위 API **활용신청** (자동승인/심사 혼재 — 온통청년은 심사 승인이므로 여유를 둘 것)
2. 발급된 **활용가이드 문서**에서 확정할 것:
   - 오퍼레이션 목록 (목록조회 / 상세조회 분리 여부)
   - **서비스 고유 ID 필드명** ← 전체 설계의 primary key. 가장 중요
   - 응답 필드명 (지원대상 / 지원내용 / 신청기한 / 신청방법 / 소관기관 / 지역 / 구비서류)
   - **일일 트래픽 한도** ← §6.5 수집 주기 설계에 직결
   - 라이선스(공공누리 유형)
3. 발급키를 GitHub Secrets에 `DATA_GO_KR_API_KEY` 등으로 등록

> **설계상의 안전장치:** §4의 레코드 스키마는 자체 정의 필드명을 쓰고, 원천 API 필드는 어댑터(`scripts/collect/adapters/*.py`)에서 매핑한다. 따라서 실제 필드명이 무엇으로 밝혀지든 **어댑터만 고치면 되고 나머지 설계는 영향받지 않는다.**

---

## 4. 데이터 모델

### 4.1 제도 레코드 (`_data/programs/{id}.json`)

```json
{
  "id": "bojo24-000123456",
  "source": "bojo24",
  "source_id": "000123456",
  "slug": "cheongnyeon-wolse-jiwon",

  "name": "청년월세 특별지원",
  "org": "국토교통부",
  "region": { "scope": "national", "sido": null, "sigungu": null },
  "category": "housing",
  "audience": ["youth"],

  "target_raw": "만 19~34세 무주택 청년 ...",
  "benefit_raw": "월 최대 20만 원, 최대 12개월 ...",
  "criteria_raw": "청년 가구 중위소득 60% 이하 ...",
  "how_to_raw": "복지로 온라인 신청 또는 주소지 행정복지센터 ...",
  "documents_raw": ["임대차계약서 사본", "월세 이체 증빙"],

  "apply_period": { "start": "2026-03-02", "end": "2027-02-28", "always": false },
  "apply_url": "https://www.bokjiro.go.kr/...",
  "official_url": "https://www.gov.kr/...",

  "content_hash": "sha256:9f2a...",
  "first_published": "2026-08-20",
  "last_updated": "2026-08-20",
  "last_checked": "2026-08-20",
  "revision": 1,
  "status": "active"
}
```

**필드 역할:**
- `id` — **전역 primary key.** `{source}-{source_id}`. 이것이 중복 방지의 뿌리다.
- `*_raw` — 원천 API 원문. **LLM이 건드리지 않는 사실 원본.** 페이지에 그대로도 렌더된다.
- `content_hash` — `*_raw` + `apply_period` 정규화 후 해시. **변경 감지 전용.**
- `last_checked` vs `last_updated` — 확인만 한 날과 실제로 바뀐 날을 분리. 페이지에는 "최종 확인일"로 `last_checked`를 노출한다 (YMYL 신뢰도 요소).
- `status` — `active` / `closed` / `superseded`. 마감된 제도도 **삭제하지 않고** 상태만 바꾼다(검색 유입 유지 + "종료된 제도" 안내가 그 자체로 유용).

### 4.2 발행 원장 (`_data/registry.json`)

```json
{
  "bojo24-000123456": {
    "slug": "cheongnyeon-wolse-jiwon",
    "path": "_programs/housing/cheongnyeon-wolse-jiwon.md",
    "content_hash": "sha256:9f2a...",
    "first_published": "2026-08-20",
    "last_updated": "2026-08-20",
    "revision": 1
  }
}
```

**단일 진실 공급원.** 수집·생성·갱신 모든 단계가 이 파일을 먼저 조회한다. Git에 커밋되므로 워크플로우 재실행/롤백에도 이력이 보존된다.

### 4.3 발행 큐 (`_data/queue.json`)

```json
{
  "generated_at": "2026-08-20",
  "pending": [
    { "id": "bojo24-000123456", "score": 87.5, "reason": "national+deadline_soon" },
    { "id": "welfare-c-00891",  "score": 62.0, "reason": "national" }
  ]
}
```

**우선순위 점수** (초안 — 운영하며 조정):

```
score = 30 × (전국 제도면 1, 광역 0.6, 기초 0.3)
      + 25 × (대상 범위 추정: 청년/전국민 1.0, 특수 대상 0.4)
      + 20 × (신청 마감 임박도: 30일 이내 1.0, 상시 0.5, 마감 0)
      + 15 × (신규 등록 30일 이내 1.0)
      + 10 × (제도명 검색 수요 추정: 고유명사 길이·일반명사 비율 휴리스틱)
```

---

## 5. 정보 구조 (IA)

### 5.1 URL 설계

날짜가 URL에서 **완전히 사라진다.** 이것이 뉴스 사이트와 레퍼런스 사이트를 가르는 지점이다.

| 경로 | 내용 |
|------|------|
| `/` | 홈 — 신규·마감임박·인기 제도 큐레이션 |
| `/support/{category}/{slug}/` | **제도 상세 (핵심 페이지)** |
| `/support/` | 전체 제도 인덱스 |
| `/support/{category}/` | 분야별 허브 (주거/일자리/의료/양육/교육/금융/문화) |
| `/who/{audience}/` | 대상별 허브 (청년/신혼부부/1인가구/어르신/장애인/소상공인) |
| `/region/{sido}/` | 지역별 허브 (17개 시도) |
| `/deadline/` | 마감 임박 (자동 갱신) |
| `/new/` | 최근 추가 |
| `/brief/` | **구 뉴스 아카이브** (§9 참고) |

`/support/`, `/who/`, `/region/` 허브가 **롱테일 검색을 먹는 실질적 엔진**이다. 개별 제도 페이지는 지명 검색을, 허브는 탐색형 검색("서울 청년 지원금 총정리")을 가져간다.

### 5.2 Jekyll 컬렉션 전환

`_posts/` → `_programs/` 커스텀 컬렉션으로 이동한다.

```yaml
# _config.yml
collections:
  programs:
    output: true
    permalink: /support/:categories/:name/

defaults:
  - scope: { path: "", type: "programs" }
    values: { layout: program }
```

> **가능한 이유:** 배포가 GitHub Pages 기본 빌더가 아니라 워크플로우 내 `bundle exec jekyll build` + `peaceiris/actions-gh-pages`이므로(`daily-post.yml:70,78`), **커스텀 컬렉션과 비화이트리스트 플러그인이 자유롭게 쓰인다.** 현행 구조가 준 뜻밖의 이점이다.

`_posts`는 컬렉션 특성상 파일명에 날짜가 강제되지만 `_programs`는 자유롭다. `2026-08-12-youth.md` 같은 이름이 사라지는 것 자체가 중복 방지에 기여한다.

---

## 6. 파이프라인 재설계

### 6.1 전체 흐름

```
[일 1회 / KST 07:00]

 ① 동기화 (sync)
    API 목록조회 → 레코드 정규화 → content_hash 계산
              │
              ├─ registry에 없는 id      → 신규 후보
              ├─ 있고 hash 다름          → 갱신 후보
              └─ 있고 hash 같음          → 무시 (last_checked만 갱신)
              ▼
 ② 큐 산출 (queue)
    신규 후보에 우선순위 점수 부여 → queue.json 기록
              ▼
 ③ 발행 (publish)   최대 N건 (기본 5)
    큐 상위 N건 → LLM 해설 생성 → _programs/*.md 작성 → registry 등록
              ▼
 ④ 갱신 (refresh)   최대 M건 (기본 10)
    갱신 후보 → 변경된 섹션만 재생성 → revision++ → last_updated 갱신
              ▼
 ⑤ 인덱스 재생성
    /deadline/, /new/, 허브 페이지의 _data 갱신
              ▼
 ⑥ 커밋 + Jekyll 빌드 + gh-pages 배포   (현행 워크플로우 그대로)
```

### 6.2 중복 방지 3중 장치

| 계층 | 장치 | 막는 것 |
|------|------|---------|
| 1 | `registry.json` id 조회 | 같은 제도의 재발행 (완전 중복) |
| 2 | `content_hash` 비교 | 내용 안 바뀐 제도의 무의미한 갱신 |
| 3 | 제목 정규화 + 토큰 자카드 유사도 ≥ 0.85 | 서로 다른 소스에 중복 등재된 같은 제도 (예: 보조금24와 복지로에 각각 있는 동일 사업) |

3계층은 자동 병합하지 않고 **`_data/review_needed.json`에 쌓아 사람이 확인**한다. 자동 병합은 서로 다른 제도를 잘못 합칠 위험이 있다(예: "청년월세지원"과 "청년월세 한시 특별지원"은 실제로 다른 제도다).

기존 `base.py:normalize_title()` / `dedupe_by_title()`은 1·3계층에 그대로 재사용한다.

### 6.3 LLM 역할 축소 — 환각 차단의 핵심

**현행:** 기사 본문을 던지고 제목·수치·본문을 전부 자유 생성시킨다 → *"빛의 속도를 능가하는 에너지 발견"* 같은 사고 발생.

**전환 후:** 사실은 API가, 문장은 LLM이 담당하도록 완전 분리한다.

| 요소 | 생성 주체 |
|------|-----------|
| 제도명, 지원 금액, 대상 연령, 소득 요건, 신청 기간, 소관 기관, 신청 URL, 구비서류 | **API 원본 그대로** (LLM 접근 불가) |
| 페이지 제목 | `{제도명}` 고정 템플릿 |
| 쉬운 말 요약, "나도 받을 수 있나" 해설, 신청 절차 풀어쓰기, FAQ | LLM |

프롬프트에 **"제공된 필드에 없는 수치·조건은 어떤 경우에도 생성하지 말 것"** 을 넣고, 생성 후 **후검증**을 건다: 결과물에서 숫자·금액·연령 토큰을 추출해 원본 `*_raw`에 존재하지 않는 값이 있으면 **해당 문장을 폐기**하고 재생성한다.

이 검증은 자유 서술형 뉴스에서는 원리적으로 불가능했지만, 정형 필드 기반에서는 가능하다. **컨셉 전환이 품질 문제까지 함께 푸는 지점이다.**

### 6.4 페이지 템플릿 (`_layouts/program.html`)

```
┌ 제도명 (H1)                                    [소관기관] [지역] [상태]
├ ⚠ 본 사이트는 정부 공식 사이트가 아닙니다. 신청은 공식 창구를 이용하세요.
│
├ 한눈에 보기 ─ 표 (API 원본)
│   대상 / 지원 내용 / 신청 기간 / 신청처 / 소관
│
├ 나도 받을 수 있나요? ─ 체크리스트        [LLM 해설 + 원본 요건]
├ 얼마를 받나요? ─ 금액 카드                [API 원본]
├ 어떻게 신청하나요? ─ 단계                 [LLM 해설]
├ 필요한 서류 ─ 리스트                      [API 원본]
├ 자주 묻는 질문 ─ 아코디언                 [LLM]
│
├ 함께 보면 좋은 제도 ─ 같은 category/audience 자동 3~5건   ← 내부링크 SEO
│
└ 출처: {기관} · 공공누리 제1유형 · 최종 확인 {last_checked}
   [공식 페이지에서 확인하기 →]
```

기존 `.cn-*` 카드뉴스 컴포넌트(`generate_post.py:_render_components`)의 **CSS와 렌더 구조를 거의 그대로 재사용**할 수 있다. `cn-stats`(금액 카드), `cn-table`(한눈에 보기), `cn-check`(체크리스트), `cn-timeline`(신청 일정), `cn-faq`, `cn-source` 전부 이 템플릿에 1:1 대응한다. **가장 손이 덜 가는 부분이다.**

### 6.5 발행량 정책

- 신규 **하루 5건**, 갱신 **하루 10건** 상한
- 큐가 비면 **발행하지 않는다** (현행의 "매일 4개 강제"가 중복의 직접 원인이었다)
- API 일일 트래픽 한도(§3.2에서 확인 필요)를 초과하지 않도록 목록조회는 페이지네이션으로 나눠 **주 1회 전체 동기화 + 매일 증분 동기화**로 운영

---

## 7. 기존 자산 처리

### 7.1 재사용 / 수정 / 폐기

| 자산 | 처리 |
|------|------|
| `assets/css/main.css` | **거의 그대로.** 카테고리 컬러 변수와 `.cn-*` 컴포넌트만 신규 분류에 맞게 확장 |
| `assets/js/main.js` | 그대로 (다크모드·탭 필터) |
| `_layouts/default.html`, `_includes/header|footer` | 네비게이션 링크만 교체 |
| `_layouts/post.html` | `_layouts/program.html`로 복제 후 개조 |
| `generate_post.py:_render_components()` | **핵심 재사용.** 입력 스키마만 교체 |
| `generate_post.py:_build_prompt()` | 전면 재작성 (§6.3) |
| `generate_post.py:_strip_foreign()`, `_esc()`, `_yaml_safe()` | 그대로 |
| `collect/base.py` | `fetch`/`retry`/`normalize_title`/`dedupe_by_title` 유지, RSS 파서는 미사용 |
| `collect/korea_policy.py`, `collect/curious.py` | **폐기** (RSS 최신순 = 문제의 근원) |
| `run_all.py` | 전면 재작성 (§6.1의 6단계) |
| `search.html` / `search.json` | 그대로 (인덱스 대상만 `site.programs`로 교체) |
| `archive.html` | `/support/` 전체 인덱스로 개조 |
| `category-*.html` | 신규 분류 체계로 교체 |
| `.github/workflows/daily-post.yml` | 워크플로우명·env만 수정. **빌드·배포 스텝은 그대로** |
| 법적 페이지 (`privacy`/`terms`/`disclaimer`/`about`) | 유지 + 면책 문구 강화 |
| GA4 / Search Console / AdSense 설정 | **전부 유지** (도메인 불변) |

### 7.2 기존 272개 포스트 마이그레이션

**삭제하지 않는다.** 이유:
- 이미 색인된 URL을 대량 삭제하면 404가 급증해 사이트 전체 평가가 흔들린다
- AdSense 승인 상태에서 콘텐츠가 급감하면 재심사 리스크가 있다

**처리 방안:**

1. **완전 중복 42건 정리** — 같은 제목 그룹에서 가장 내용이 충실한 1건만 남기고, 나머지는 남긴 글로 `canonical` 지정 (삭제 대신 정규화)
2. **나머지는 `/brief/` 아카이브로 격리** — `_posts` 유지, permalink만 `/brief/:year/:month/:day/:title/`로 변경하고 301 리디렉션 매핑을 `_redirects` 또는 메타 리프레시로 제공
3. **홈·검색·사이트맵에서 제외 또는 후순위화** — 신규 `_programs`가 사이트의 얼굴이 되도록
4. **뉴스 발행은 즉시 중단** — 전환 첫날부터 `daily-post.yml`은 신규 파이프라인만 실행

> 대안: 아카이브 전체 `noindex` 처리. 다만 트래픽이 실제로 나오는 글이 일부 있다면 손해이므로, **Search Console에서 유입 있는 글을 먼저 확인한 뒤 결정**할 것. 이건 데이터를 봐야 하는 판단이다.

---

## 8. SEO·수익화 고려사항

### 8.1 YMYL 대응 (필수)

지원금·복지는 **YMYL(Your Money or Your Life) 영역**이라 검색엔진이 신뢰도를 엄격히 평가한다. 다음은 선택이 아니라 필수다.

- 모든 제도 페이지에 **출처 기관 + 공식 페이지 링크 + 최종 확인일** 노출
- **"정부 공식 사이트 아님" 고지** 3중 노출 (§2.4)
- 금액·요건을 단정하지 않고 "공식 창구에서 최종 확인" 안내 병기
- 마감된 제도는 페이지 상단에 **"종료된 제도입니다"** 배너

### 8.2 구조화 데이터

제도 페이지에 `GovernmentService` 또는 `Service` JSON-LD를 넣는다. `provider`(소관기관), `areaServed`(지역), `audience`, `dateModified`를 채우면 리치 결과 대상이 된다. 뉴스 `Article` 스키마보다 이 사이트 성격에 훨씬 맞는다.

### 8.3 내부링크

허브(분야/대상/지역) ↔ 제도 상세를 양방향으로 자동 연결한다. 제도가 1,000건 쌓이면 자동 생성된 내부링크만 수천 개가 되고, 이것이 롱테일 색인의 동력이 된다. 현행 뉴스 구조에는 존재하지 않던 자산이다.

---

## 9. 리스크와 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| API 활용신청 심사 지연·반려 | **높음** | 파일데이터(CSV, `15083323`)로 초기 시드 구축 후 API 전환. 설계상 어댑터 교체만으로 대응됨 |
| 응답 필드명이 예상과 다름 | 중 | 어댑터 계층(§3.2)이 흡수. 스키마 불변 |
| 보조금24·복지로와의 정면 경쟁 | 중 | 공식 포털은 UX가 나쁘고 해설이 없다. **"쉬운 말 + 조건 체크리스트 + 유사 제도 비교"** 로 롱테일에서 차별화 |
| 자동 생성 콘텐츠에 대한 검색엔진 평가 | **높음** | §6.3의 사실/문장 분리 + 후검증. 사실은 공공 원본, 문장만 생성 → 단순 스팸과 구분되는 구조 |
| 잘못된 지원금 정보로 인한 민원 | 중 | 면책 고지 + 최종 확인일 + 공식 링크. 금액·요건은 원본 그대로만 렌더 |
| Groq 무료 한도 | 낮음 | 발행량이 하루 5+10건으로 통제됨. 현행(4건)보다 늘지만 폴백 로직 이미 존재 |
| 지자체 데이터 품질 편차 | 중 | 필수 필드(대상/내용/신청처) 누락 시 큐에서 제외하고 `_data/incomplete.json`에 격리 |

---

## 10. 구현 로드맵

| 단계 | 내용 | 산출물 | 의존성 |
|------|------|--------|--------|
| **0** | API 활용신청·키 발급·필드 확정 | Secrets 등록, 활용가이드 | **사용자 작업** ⚠️ |
| **1** | 데이터 레이어 | `collect/adapters/*.py`, `sync.py`, `registry.py`, `queue.py` | 0 |
| **2** | 콘텐츠 레이어 | `_programs` 컬렉션, `_layouts/program.html`, 렌더러 | 1 |
| **3** | 생성 레이어 | 새 프롬프트, 후검증기 | 1, 2 |
| **4** | 허브·인덱스 | `/support/`, `/who/`, `/region/`, `/deadline/`, `/new/` | 2 |
| **5** | 기존 포스트 마이그레이션 | `/brief/` 격리, canonical 정리, 리디렉션 | 4 |
| **6** | 워크플로우 전환 | `daily-post.yml` → `daily-sync.yml` | 1~5 |
| **7** | 초기 백필 | 상위 100~300건 일괄 발행 | 6 |

**단계 0이 전체의 병목이다.** 1~7은 코드 작업이므로 이어서 진행 가능하지만, 0 없이는 실데이터 검증이 불가능하다.

> **단계 0 없이 착수하는 방법:** 어댑터에 목(mock) 응답을 넣어 1~5를 먼저 구현하고, 키 발급 후 어댑터만 실데이터로 교체. 설계가 어댑터 패턴이므로 이 경로가 유효하다. 착수 시점을 앞당기고 싶다면 권장.

---

## 11. 결정이 필요한 사항

### 확정된 것

| 항목 | 결정 | 반영 위치 |
|------|------|-----------|
| 사이트명 | **지원금 도감** | `_config.yml` `title` |
| 분야 분류 | **자체 재매핑** (분야 7 · 대상 8 · 지역 18) | `scripts/taxonomy.py` |
| 단계 0 대기 | **기다리지 않고 목 데이터로 선구현** | `collect/adapters/mock.py` |
| 기존 272개 포스트 | **`/brief/` 격리 확정** — `noindex`·삭제 모두 채택하지 않음 | `scripts/migrate_brief.py` |
| 레포 공개 범위 | **public 유지** | 아래 참고 |
| 초기 범위 | **중앙부처(전국) 우선** | `sync.py:DEFAULT_SCOPES` |

**기존 포스트를 `/brief/` 격리로 확정한 이유:** 삭제는 404를 대량 발생시키고, 전체 `noindex`는
유입이 남아 있는 글까지 버린다. 격리는 둘 다 피하면서 중복 신호만 제거한다(완전 중복 42건은
canonical 로 정본에 합쳤다). 나중에 Search Console 에서 유입이 0으로 확인되면 그때
`noindex` 로 좁히면 되고, 그 반대(지우고 나서 되살리기)는 불가능하다.

**레포를 public 으로 유지하는 이유:** GitHub Pages 무료 플랜은 public 레포에서만 사이트를
게시한다. private 으로 바꾸면 사이트가 내려간다. 반면 private 으로 얻는 보호는 없다 —
GA4 ID·AdSense 게시자 ID·Search Console 인증 토큰은 배포된 모든 페이지의 HTML 에 이미
들어 있고, `ads.txt` 는 공개 접근이 존재 이유다. 실제 비밀(`GROQ_API_KEY`,
`DATA_GO_KR_API_KEY`)은 GitHub Actions Secrets 에만 있으며 레포 공개 여부와 무관하게
노출되지 않는다. 워크플로우 트리거도 `schedule` 과 `workflow_dispatch` 뿐이라
포크 PR 로 시크릿이 새는 경로가 없다.

**중앙부처 우선으로 정한 이유:** 데이터 품질이 균일하고, 지자체는 건수가 많아 초기에
품질 관리가 어렵다. 이 결정이 단계 0의 작업량을 절반으로 줄인다 — API 4개가 아니라
**2개만 활용신청**하면 된다.

| API | 데이터셋 | 단계 0에서 신청 |
|-----|----------|-----------------|
| 행정안전부_대한민국 공공서비스(혜택) 정보 (보조금24) | `15113968` | ✅ |
| 한국사회보장정보원_중앙부처복지서비스 | `15090532` | ✅ |
| 한국사회보장정보원_지자체복지서비스 | `15108347` | 나중에 |
| 한국고용정보원_온통청년_청년정책 | `15143273` | 나중에 |

구현상 처리:

- `sync.py` 가 `REGION_SCOPE`(기본 `national`)로 발행 범위를 거른다. 범위 밖 제도는
  **버리는 게 아니라 발행만 보류**한다. 원천에서 매번 다시 읽으므로
  `REGION_SCOPE="national,sido,sigungu"` 로 바꾸면 그날 신규로 잡혀 그대로 발행된다.
- **이미 발행된 제도는 범위와 무관하게 계속 추적한다.** 범위를 좁혔다고 살아 있는
  페이지를 방치하면 정보가 낡은 채로 남는다.
- 지자체 제도가 없는 단계에서 시도 허브 17개를 만들면 빈 페이지만 늘어 색인에 해롭다.
  `_plugins/hub_generator.rb` 가 **제도가 실제로 있는 시도만** 허브를 만들고,
  `_layouts/hub.html` 의 탐색바도 같은 기준으로 링크를 건다(안 그러면 404).

### 남은 결정

없음. 단계 0(API 활용신청)부터는 사용자 작업이다.

---

## 부록: 새 디렉터리 구조 (안)

```
walapp/
├── _config.yml                    # collections.programs 추가, title/description 교체
├── _data/
│   ├── registry.json              # 발행 원장 ★
│   ├── queue.json                 # 발행 큐
│   ├── review_needed.json         # 유사도 검토 대기
│   ├── incomplete.json            # 필수 필드 누락 격리
│   └── programs/{id}.json         # 제도 원본 레코드
│
├── _programs/                     # ★ 새 콘텐츠 루트
│   ├── housing/*.md
│   ├── jobs/*.md
│   ├── health/*.md
│   └── ...
│
├── _posts/                        # 구 뉴스 (동결, /brief/로 격리)
│
├── _layouts/
│   ├── default.html               # 유지
│   ├── program.html               # ★ 신규
│   ├── hub.html                   # ★ 신규 (분야/대상/지역 공용)
│   └── post.html                  # 구 뉴스용 유지
│
├── support/, who/, region/        # 허브 페이지
├── deadline.html, new.html
│
└── scripts/
    ├── run_all.py                 # 6단계 오케스트레이션으로 재작성
    ├── sync.py                    # ★ ① 동기화
    ├── queue.py                   # ★ ② 큐 산출
    ├── publish.py                 # ★ ③ 발행
    ├── refresh.py                 # ★ ④ 갱신
    ├── registry.py                # ★ 원장 R/W
    ├── verify.py                  # ★ 생성물 사실 후검증
    ├── render.py                  # 구 _render_components 이관
    └── collect/
        ├── base.py                # fetch/normalize 유지
        └── adapters/              # ★ 원천 API → 표준 스키마
            ├── bojo24.py
            ├── welfare_central.py
            ├── welfare_local.py
            └── youth.py
```
