# Walapp 설계서 — 지원금·제도 도감

> 작성일: 2026-08-12 · 최종 갱신: 2026-08-18
> 상태: **전환 완료.** 설계 단계 0~7 종료, 매일 자동 발행 중.
> 이 문서는 **왜 지금 구조인가**를 적는다. 무엇이 어떻게 돌아가는지는
> `docs/IMPLEMENTATION.md`(구현 현황·실행 방법·발행 정책)에 있다.

---

## 1. 왜 이 구조인가 — 출발점

이 사이트는 원래 매일 뉴스를 요약해 발행하는 브리핑 사이트였다. 그 글 272건은
2026-08-18 자로 전량 걷어냈다(§7). 다만 **왜 걷어냈는지는 지금 구조의 근거**라
여기 남긴다. 아래 세 가지가 지금의 모든 설계 결정을 낳았다.

**① 최신순으로 소재를 뽑으면 반드시 겹친다.**
RSS 를 최신순으로 받아 상위 N건을 쓰면, 뉴스 사이클이 같은 이슈를 며칠씩 반복
노출하는 만큼 어제와 겹친다. 실제로 제목이 완전히 같은 글이 15.4%(42건)였고,
의미 중복은 그보다 훨씬 높았다.
→ **그래서 발행 단위를 '날짜'가 아니라 '제도'로 바꿨다.** 같은 제도는 항상 같은
경로에 쓰이므로 재수집이 새 글이 아니라 갱신이 된다(§2.2, §5).

**② 중복 제거가 그날 수집분 안에서만 돌면 소용이 없다.**
어제 발행한 글의 존재를 모르는 채로 중복을 지웠다. 생성 프롬프트에도 발행 이력이
들어가지 않아 LLM 은 매일 백지에서 시작했다.
→ **그래서 발행 원장(`_data/registry.json`)을 단일 진실 공급원으로 두고, 수집
단계에서 원장과 대조한다**(§6.2). 원장을 지우면 중복 방지가 통째로 무너진다.

**③ 자유 서술형 요약은 환각을 막을 구조가 없다.**
구 브리핑에는 *"빛의 속도를 능가하는 에너지 발견"* 같은, 물리적으로 성립하지 않는
제목이 실렸다. 지원금은 YMYL 영역이라 같은 사고가 실제 피해가 된다.
→ **그래서 사실과 문장을 갈랐다.** 금액·자격·기간·기관은 공공데이터 원문을 그대로
옮기고, LLM 은 그 문장을 쉬운 말로 푸는 일만 한다. 원문에 없는 수치가 생성문에
들어가면 `verify.py` 가 그 문장을 폐기한다(§6.3, §8.1).

덧붙여, 1차 취재 없는 뉴스 요약은 원문(korea.kr·연합뉴스)이 늘 위에 있어 SEO 가치가
사실상 0이었고 수명이 48시간이었다. 제도 페이지는 반대다 — 한 번 쓰면 갱신하며
쌓인다.

---

## 2. 컨셉 정의

### 2.1 한 줄 정의

> **대한민국의 모든 정부·지자체 지원 제도를, 제도 하나당 페이지 하나로 정리한 사전(백과).**

뉴스가 아니라 **레퍼런스 사이트**다. 발행 단위가 "오늘"에서 "제도"로 바뀐다.

### 2.2 전환의 핵심 — 글의 정체성(primary key) 변경

| | 현행 | 전환 후 |
|---|------|---------|
| 글의 키 | `날짜 + 카테고리` (`2026-08-12-youth.md`) | `제도 ID` (`youth-monthly-rent-support`) |
| 같은 주제 재등장 시 | **새 글 생성** → 중복 | **기존 글 갱신** → 중복 불가 |
| 소재 선택 | RSS 최신순 | 사전 구축된 큐에서 우선순위순 |
| 수명 | 48시간 | 영구 (제도 폐지 시까지) |
| 발행량의 상한 | 없음 (매일 4개 강제) | 큐 소진 시 발행 없음 (억지 생성 금지) |

**중복이 불가능한 이유는 규칙이 아니라 자료구조다.** 같은 제도는 같은 ID를 갖고, 같은 ID는 파일 경로가 같으므로, 두 번째 시도는 새 글이 아니라 덮어쓰기(=갱신)가 된다.

### 2.3 왜 이 주제인가

- **주제 우주가 유한하면서 충분히 크다** — 보조금24 기준 수천 건, 지자체 복지서비스까지 포함하면 1만 건 이상. 하루 5건씩 써도 수년치.
- **에버그린 + 갱신 수요 동시 충족** — 제도는 매년 금액·요건이 바뀐다. 갱신할 때마다 `dateModified`가 갱신돼 검색엔진이 신선도를 인정한다. 뉴스처럼 죽지 않으면서 뉴스처럼 신선하다.
- **검색 의도가 명확하고 상업적이다** — "청년월세지원 조건", "근로장려금 신청방법" 류의 고전환 롱테일. 광고 단가가 뉴스 대비 크게 높다.
- **AI에게 시키기 가장 안전한 작업이다** — 이미 구조화된 API 필드를 "쉬운 말로 풀어쓰기"만 시킨다. 사실·수치는 원본을 그대로 렌더한다. §6.3 참고.
- **기존 자산 재활용률이 높다** — 다크모드·검색·배포 워크플로우·법적 페이지가 그대로 쓰인다. 갈아엎은 것은 수집과 렌더뿐이다.

### 2.4 사이트 정체성

- **도메인·레포명 유지** (`adk24211.github.io/walapp`) — 변경 비용 대비 실익 없음
- `_config.yml`의 `title`/`description`은 교체
  - `title`: `지원금 도감` (안)
  - `description`: `정부·지자체 지원금과 복지 제도를, 받을 수 있는지부터 신청 방법까지 하나씩 정리합니다.`
- **필수 고지 강화**: "본 사이트는 정부 공식 사이트가 아니며, 신청은 반드시 공식 창구를 이용하십시오." — 지원금 정보 사이트는 정부 사칭 오해 소지가 있으므로 헤더·푸터·각 제도 페이지 3곳에 노출한다.

---

## 3. 데이터 소스

### 3.1 확인된 소스 (2026-08-12 기준 존재 확인)

| 소스 | 공공데이터포털 ID | 성격 | 역할 |
|------|------------------|------|------|
| 행정안전부_대한민국 공공서비스(혜택) 정보 (보조금24) | `15113968` | Open API | **주 소스.** 중앙부처·지자체·공공기관·교육청 수혜서비스 목록 + 상세 |
| 한국사회보장정보원_중앙부처복지서비스 | `15090532` | Open API | 복지로 기반 중앙부처 복지사업 |
| 한국사회보장정보원_지자체복지서비스 | `15108347` | Open API | 지자체 복지서비스 (지역 롱테일의 핵심) |
| 한국고용정보원_온통청년_청년정책 | `15143273` | Open API | 청년정책 전용 |
| 한국사회보장정보원_복지서비스정보 | `15083323` | 파일(CSV) | **폴백.** API 승인 지연 시 초기 시드용 |
| 기업마당 지원사업정보 | `bizinfo.go.kr/apiDetail.do?id=bizinfoApi` | Open API | (선택) 소상공인·기업 지원 확장 시 |

### 3.2 현재 붙어 있는 것

| 소스 | 상태 |
|------|------|
| 보조금24 (`15113968`) | ✅ **주 소스.** 활용가이드로 필드 확정. 목록+상세를 서비스ID 로 병합 |
| 중앙부처복지서비스 (`15090532`) | 구현되어 있으나 기본 꺼짐 (`ENABLE_WELFARE_CENTRAL=1` 로 켠다) |
| 나머지 | 미착수. 지자체·청년정책은 범위를 넓힐 때 |

작성 시점에는 이그레스 프록시가 `data.go.kr` 을 막아 응답 필드명을 확정하지 못한 채
설계했다. 그 제약이 남긴 설계가 지금도 유효하다 — **§4 의 레코드 스키마는 자체 필드명을
쓰고, 원천 필드는 어댑터(`scripts/collect/adapters/*.py`)에서 매핑한다.** 원천이 무엇으로
바뀌든 어댑터만 고치면 된다. 새 API 의 필드명을 조사하는 도구는 `scripts/inspect_api.py`.

> 키는 GitHub Secrets 의 `DATA_GO_KR_API_KEY` 에만 있다. **어떤 파일·커밋·산출물에도
> 쓰지 않는다.** 키가 없으면 워크플로우 첫 스텝이 실행을 중단시킨다 — 목 데이터가
> 배포되면 가짜 지원금 수치가 공개되기 때문이다.

---

## 4. 데이터 모델

### 4.1 제도 레코드 (`_records/{id}.json`)

```json
{
  "id": "bojo24-000123456",
  "source": "bojo24",
  "source_id": "000123456",
  "slug": "cheongnyeon-wolse-jiwon",

  "name": "청년월세 특별지원",
  "org": "국토교통부",
  "region": { "scope": "national", "sido": null, "sigungu": null },
  "category": "housing",
  "audience": ["youth"],

  "target_raw": "만 19~34세 무주택 청년 ...",
  "benefit_raw": "월 최대 20만 원, 최대 12개월 ...",
  "criteria_raw": "청년 가구 중위소득 60% 이하 ...",
  "how_to_raw": "복지로 온라인 신청 또는 주소지 행정복지센터 ...",
  "documents_raw": ["임대차계약서 사본", "월세 이체 증빙"],

  "apply_period": { "start": "2026-03-02", "end": "2027-02-28", "always": false },
  "apply_url": "https://www.bokjiro.go.kr/...",
  "official_url": "https://www.gov.kr/...",

  "content_hash": "sha256:9f2a...",
  "first_published": "2026-08-20",
  "last_updated": "2026-08-20",
  "last_checked": "2026-08-20",
  "revision": 1,
  "status": "active"
}
```

**필드 역할:**
- `id` — **전역 primary key.** `{source}-{source_id}`. 이것이 중복 방지의 뿌리다.
- `*_raw` — 원천 API 원문. **LLM이 건드리지 않는 사실 원본.** 페이지에 그대로도 렌더된다.
- `content_hash` — `*_raw` + `apply_period` 정규화 후 해시. **변경 감지 전용.**
- `last_checked` vs `last_updated` — 확인만 한 날과 실제로 바뀐 날을 분리. 페이지에는 "최종 확인일"로 `last_checked`를 노출한다 (YMYL 신뢰도 요소).
- `status` — `active` / `closed` / `superseded`. 마감된 제도도 **삭제하지 않고** 상태만 바꾼다(검색 유입 유지 + "종료된 제도" 안내가 그 자체로 유용).

### 4.2 발행 원장 (`_data/registry.json`)

```json
{
  "bojo24-000123456": {
    "slug": "cheongnyeon-wolse-jiwon",
    "path": "_programs/housing/cheongnyeon-wolse-jiwon.md",
    "content_hash": "sha256:9f2a...",
    "first_published": "2026-08-20",
    "last_updated": "2026-08-20",
    "revision": 1
  }
}
```

**단일 진실 공급원.** 수집·생성·갱신 모든 단계가 이 파일을 먼저 조회한다. Git에 커밋되므로 워크플로우 재실행/롤백에도 이력이 보존된다.

### 4.3 발행 큐 (`_data/queue.json`)

```json
{
  "generated_at": "2026-08-20",
  "pending": [
    { "id": "bojo24-000123456", "score": 87.5, "reason": "national+deadline_soon" },
    { "id": "welfare-c-00891",  "score": 62.0, "reason": "national" }
  ]
}
```

**우선순위 점수** (초안 — 운영하며 조정):

```
score = 30 × (전국 제도면 1, 광역 0.6, 기초 0.3)
      + 25 × (대상 범위 추정: 청년/전국민 1.0, 특수 대상 0.4)
      + 20 × (신청 마감 임박도: 30일 이내 1.0, 상시 0.5, 마감 0)
      + 15 × (신규 등록 30일 이내 1.0)
      + 10 × (제도명 검색 수요 추정: 고유명사 길이·일반명사 비율 휴리스틱)
```

---

## 5. 정보 구조 (IA)

### 5.1 URL 설계

날짜가 URL 에 없다. 이것이 뉴스 사이트와 레퍼런스 사이트를 가르는 지점이다.

| 경로 | 내용 |
|------|------|
| `/` | 홈 — 신규·마감임박·인기 제도 큐레이션 |
| `/support/{category}/{slug}/` | **제도 상세 (핵심 페이지)** |
| `/support/` | 전체 제도 인덱스 |
| `/support/{category}/` | 분야별 허브 (주거/일자리/의료/양육/교육/금융/문화) |
| `/who/{audience}/` | 대상별 허브 (청년/신혼부부/1인가구/어르신/장애인/소상공인) |
| `/region/{sido}/` | 지역별 허브 (17개 시도) |
| `/deadline/` | 마감 임박 (자동 갱신) |
| `/new/` | 최근 추가 |

`/support/`, `/who/`, `/region/` 허브가 **롱테일 검색을 먹는 실질적 엔진**이다. 개별 제도 페이지는 지명 검색을, 허브는 탐색형 검색("서울 청년 지원금 총정리")을 가져간다.

### 5.2 Jekyll 컬렉션

제도는 `_programs/` 커스텀 컬렉션이다.

```yaml
# _config.yml
collections:
  programs:
    output: true
    permalink: /support/:path/

defaults:
  - scope: { path: "", type: "programs" }
    values: { layout: program }
```

> **가능한 이유:** 배포가 GitHub Pages 기본 빌더가 아니라 워크플로우 안의
> `bundle exec jekyll build` + `peaceiris/actions-gh-pages` 다. 그래서 커스텀 컬렉션은
> 물론 비화이트리스트 플러그인(`_plugins/hub_generator.rb`)도 자유롭게 쓴다.

`permalink: /support/:path/` 라 **파일 경로가 곧 URL** 이다. 별도 매핑이 없다.
표준 `_posts` 컬렉션은 파일명에 날짜를 강제하지만 `_programs` 는 자유로우므로,
같은 제도가 항상 같은 파일에 쓰인다 — 재수집이 새 글이 아니라 갱신이 되는 지점이다.

---

## 6. 파이프라인

### 6.1 전체 흐름

```
[일 1회 / KST 07:00]

 ① 동기화 (sync)
    API 목록조회 → 레코드 정규화 → content_hash 계산
              │
              ├─ registry에 없는 id      → 신규 후보
              ├─ 있고 hash 다름          → 갱신 후보
              └─ 있고 hash 같음          → 무시 (last_checked만 갱신)
              ▼
 ② 큐 산출 (queue)
    신규 후보에 우선순위 점수 부여 → queue.json 기록
              ▼
 ③ 발행 (publish)   최대 N건 (기본 5)
    큐 상위 N건 → LLM 해설 생성 → _programs/*.md 작성 → registry 등록
              ▼
 ④ 갱신 (refresh)   최대 M건 (기본 10)
    갱신 후보 → 변경된 섹션만 재생성 → revision++ → last_updated 갱신
              ▼
 ⑤ 인덱스 재생성
    /deadline/, /new/, 허브 페이지의 _data 갱신
              ▼
 ⑥ 커밋 + Jekyll 빌드 + gh-pages 배포   (현행 워크플로우 그대로)
```

### 6.2 중복 방지 3중 장치

| 계층 | 장치 | 막는 것 |
|------|------|---------|
| 1 | `registry.json` id 조회 | 같은 제도의 재발행 (완전 중복) |
| 2 | `content_hash` 비교 | 내용 안 바뀐 제도의 무의미한 갱신 |
| 3 | 제목 정규화 + 토큰 자카드 유사도 ≥ 0.85 | 서로 다른 소스에 중복 등재된 같은 제도 (예: 보조금24와 복지로에 각각 있는 동일 사업) |

3계층은 자동 병합하지 않고 **`_data/review_needed.json`에 쌓아 사람이 확인**한다. 자동 병합은 서로 다른 제도를 잘못 합칠 위험이 있다(예: "청년월세지원"과 "청년월세 한시 특별지원"은 실제로 다른 제도다).

기존 `base.py:normalize_title()` / `dedupe_by_title()`은 1·3계층에 그대로 재사용한다.

### 6.3 LLM 역할 축소 — 환각 차단의 핵심

**현행:** 기사 본문을 던지고 제목·수치·본문을 전부 자유 생성시킨다 → *"빛의 속도를 능가하는 에너지 발견"* 같은 사고 발생.

**전환 후:** 사실은 API가, 문장은 LLM이 담당하도록 완전 분리한다.

| 요소 | 생성 주체 |
|------|-----------|
| 제도명, 지원 금액, 대상 연령, 소득 요건, 신청 기간, 소관 기관, 신청 URL, 구비서류 | **API 원본 그대로** (LLM 접근 불가) |
| 페이지 제목 | `{제도명}` 고정 템플릿 |
| 쉬운 말 요약, "나도 받을 수 있나" 해설, 신청 절차 풀어쓰기, FAQ | LLM |

프롬프트에 **"제공된 필드에 없는 수치·조건은 어떤 경우에도 생성하지 말 것"** 을 넣고, 생성 후 **후검증**을 건다: 결과물에서 숫자·금액·연령 토큰을 추출해 원본 `*_raw`에 존재하지 않는 값이 있으면 **해당 문장을 폐기**하고 재생성한다.

이 검증은 자유 서술형 뉴스에서는 원리적으로 불가능했지만, 정형 필드 기반에서는 가능하다. **컨셉 전환이 품질 문제까지 함께 푸는 지점이다.**

### 6.4 페이지 템플릿 (`_layouts/program.html`)

```
┌ 히어로 ─ 브레드크럼 · 상태 배지 · 제도명(H1) · 한 줄 요약
│          └ 정부 공식 사이트가 아니라는 고지 한 줄
│
├ 오른쪽 레일(넓은 화면) / 히어로 아래(좁은 화면)
│   지원 규모 · 신청 기간 · [공식 창구에서 신청] · 목차
│
├ 01 지원 내용            [원문 그대로 · 금액이 적힌 항목]
├ 02 나도 받을 수 있나요?  [본 사이트가 쓴 문장 + 원문 요건]
├ 03 어떻게 신청하나요?    [본 사이트가 쓴 문장 · 준비 서류 포함]
├ 04 지원 대상·선정 기준   [원문 그대로 · 길면 접기]
├ 05 신청 창구            [원문 그대로 · 표 + 소관 기관 안내 링크]
├ 06 자주 묻는 질문        [본 사이트가 쓴 문장 · 위에서 답한 질문은 제외]
│
├ 함께 보면 좋은 제도 ─ 같은 category/audience 자동 3~5건   ← 내부링크 SEO
├ 이 제도 공유하기 ─ 링크 복사 · 카카오톡 · 인스타그램
└ 출처표시 ─ 어느 데이터셋에서 왔고, 어디까지가 원문이고,
             어디부터가 본 사이트가 쓴 문장이며, 언제 확인했는가
```

섹션마다 **원문인지 본 사이트가 쓴 문장인지 라벨을 단다.** 지원금 페이지에서 이 구분이
안 보이면 읽는 사람이 어디까지 믿어야 할지 알 수 없다.

공공누리 유형은 적지 않는다 — 데이터셋마다 다르고, 확인하지 않은 값을 적을 수 없다.

### 6.5 발행량 정책

- **큐가 비면 발행하지 않는다.** 구 브리핑의 "매일 4개 강제"가 중복의 직접 원인이었다.
- 실제 운영값은 테마 조별 발행이다 — 확정된 수치와 순서 기준은
  `docs/IMPLEMENTATION.md#발행-정책` 에 있다. 여기 숫자를 적으면 두 곳이 어긋난다.
- 목록조회는 페이지네이션으로 나누고, `SYNC_SINCE` 로 증분 동기화한다
  (전량 조회 시 API 호출 220회 → 16회).

---

## 7. 구 브리핑 자산 처리 — 전량 제거 (2026-08-18)

### 7.1 무엇을 지웠나

| 대상 | 내용 |
|------|------|
| 포스트 | `_posts/` 272건 |
| 레이아웃·include | `_layouts/post.html`, `category-page`·`category-label`·`reading-time`·`sidebar` |
| 페이지 | `/brief/` `/archive/` `/weekly/` `/guide/` `/category/*` |
| 스크립트 | `migrate_brief.py` |
| 설정 | `/brief/` permalink, `categories_label`, jekyll-paginate, 페이지네이션 |
| 그 외 | 검색 색인의 브리핑 블록, 푸터·404·소개의 안내 문구, 안 쓰이는 CSS 선택자 139개 |

RSS 는 `feed.xml` 을 직접 쓰는 쪽으로 바꿨다. jekyll-feed 의 기본 피드는 `_posts`
전용이라 포스트를 지우면 빈 피드가 되는데, `feed.collections` 로 제도를 넣으면 별도
경로로 나가 기존 구독 주소(`/feed.xml`)가 끊긴다.

### 7.2 왜 격리에서 제거로 뒤집었나

전환 초기에는 **`/brief/` 격리**로 결정했었다. 근거는 두 가지였다 — 색인된 URL 을
대량 삭제하면 404 가 급증하고, AdSense 승인 상태에서 콘텐츠가 급감하면 재심사
리스크가 있다.

둘 다 더 이상 유효하지 않다.

- **AdSense 는 껐다.** 크롬이 목록 화면에 소셜 엔지니어링 인터스티셜을 띄웠고,
  재심사 기간 동안 자동 광고를 중단했다(`_config.yml` 의 `ads_enabled`).
  광고를 근거로 콘텐츠를 붙들고 있을 이유가 없어졌다.
- **404 증가는 컨셉 전환의 비용으로 받아들였다.** 브리핑 글은 유입이 사실상 없었고,
  남겨 둔 272건이 오히려 사이트 정체성을 흐렸다. 지금 사이트가 무엇인지 한 문장으로
  말할 수 없으면 사칭 사이트 분류에서도 불리하다.

`404.html` 이 검색과 전체 목록으로 안내한다. 되돌릴 필요가 생기면 git 이력에 있다.

### 7.3 남아 있는 구 자산

| 자산 | 상태 |
|------|------|
| `assets/js/main.js` | 그대로 (다크모드) |
| 법적 페이지 (`privacy`/`terms`/`disclaimer`/`about`) | 유지 + 문구 갱신 |
| GA4 · Search Console 인증 | 유지 (도메인 불변) |
| AdSense 게시자 ID | 유지하되 **스위치 off** — 세이프 브라우징 재심사 통과 후 복구 |
| `.cn-*` 본문 컴포넌트 | 유지. 브리핑과 나눠 쓰던 `.is-program` 스코프는 흔적으로 남아 있다 |

---

## 8. SEO·수익화 고려사항

### 8.1 YMYL 대응 (필수)

지원금·복지는 **YMYL(Your Money or Your Life) 영역**이라 검색엔진이 신뢰도를 엄격히 평가한다. 다음은 선택이 아니라 필수다.

- 모든 제도 페이지에 **출처 기관 + 공식 페이지 링크 + 최종 확인일** 노출
- **"정부 공식 사이트 아님" 고지** 3중 노출 (§2.4)
- 금액·요건을 단정하지 않고 "공식 창구에서 최종 확인" 안내 병기
- 마감된 제도는 페이지 상단에 **"종료된 제도입니다"** 배너

### 8.2 구조화 데이터

제도 페이지에 `GovernmentService` 또는 `Service` JSON-LD를 넣는다. `provider`(소관기관), `areaServed`(지역), `audience`, `dateModified`를 채우면 리치 결과 대상이 된다. 뉴스 `Article` 스키마보다 이 사이트 성격에 훨씬 맞는다.

### 8.3 내부링크

허브(분야/대상/지역) ↔ 제도 상세를 양방향으로 자동 연결한다. 제도가 1,000건 쌓이면 자동 생성된 내부링크만 수천 개가 되고, 이것이 롱테일 색인의 동력이 된다. 현행 뉴스 구조에는 존재하지 않던 자산이다.

---

## 9. 리스크와 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| API 활용신청 심사 지연·반려 | **높음** | 파일데이터(CSV, `15083323`)로 초기 시드 구축 후 API 전환. 설계상 어댑터 교체만으로 대응됨 |
| 응답 필드명이 예상과 다름 | 중 | 어댑터 계층(§3.2)이 흡수. 스키마 불변 |
| 보조금24·복지로와의 정면 경쟁 | 중 | 공식 포털은 UX가 나쁘고 해설이 없다. **"쉬운 말 + 조건 체크리스트 + 유사 제도 비교"** 로 롱테일에서 차별화 |
| 자동 생성 콘텐츠에 대한 검색엔진 평가 | **높음** | §6.3의 사실/문장 분리 + 후검증. 사실은 공공 원본, 문장만 생성 → 단순 스팸과 구분되는 구조 |
| 잘못된 지원금 정보로 인한 민원 | 중 | 면책 고지 + 최종 확인일 + 공식 링크. 금액·요건은 원본 그대로만 렌더 |
| Groq 무료 한도 | 낮음 | 발행량이 하루 5+10건으로 통제됨. 현행(4건)보다 늘지만 폴백 로직 이미 존재 |
| 지자체 데이터 품질 편차 | 중 | 필수 필드(대상/내용/신청처) 누락 시 큐에서 제외하고 `_data/incomplete.json`에 격리 |

---

## 10. 구현 로드맵 — 전 단계 완료

| 단계 | 내용 | 상태 |
|------|------|------|
| **0** | API 활용신청·키 발급·필드 확정 | ✅ 보조금24(`15113968`) 확정 |
| **1** | 데이터 레이어 (`collect/adapters/*`, `sync.py`, `registry.py`, `queueing.py`) | ✅ |
| **2** | 콘텐츠 레이어 (`_programs` 컬렉션, `program.html`, `render.py`) | ✅ |
| **3** | 생성 레이어 (해설 프롬프트, `verify.py` 후검증) | ✅ |
| **4** | 허브·인덱스 (`/support/` `/who/` `/region/` `/deadline/` `/new/`) | ✅ |
| **5** | 구 포스트 처리 | ✅ 격리 → **전량 제거**로 변경 (§7) |
| **6** | 워크플로우 전환 (`daily-post.yml` → `daily-sync.yml`) | ✅ 매일 KST 07:00 |
| **7** | 초기 백필 (상위 N건 일괄 발행) | 보류 — 발행 45건, 필요해지면 `PUBLISH_MODE=top` |

목 데이터로 1~5 를 먼저 짜고 키 발급 후 어댑터만 실데이터로 교체하는 경로를 썼다.
어댑터 패턴이라 가능했고, 실제로 단계 0 을 기다리지 않고 끝까지 갔다.

---

## 11. 결정이 필요한 사항

### 확정된 것

| 항목 | 결정 | 반영 위치 |
|------|------|-----------|
| 사이트명 | **지원금 도감** | `_config.yml` `title` |
| 분야 분류 | **자체 재매핑** (분야 7 · 대상 8 · 지역 18) | `scripts/taxonomy.py` |
| 단계 0 대기 | **기다리지 않고 목 데이터로 선구현** | `collect/adapters/mock.py` |
| 기존 272개 포스트 | **전량 제거** (2026-08-18). 격리 결정을 뒤집었다 — 이유는 §7.2 | — |
| 레포 공개 범위 | **public 유지** | 아래 참고 |
| 초기 범위 | **중앙부처(전국) 우선** | `sync.py:DEFAULT_SCOPES` |

**기존 포스트를 결국 제거한 이유:** 처음에는 `/brief/` 격리로 잡았다 — 삭제는 404를 대량
발생시키고, 전체 `noindex` 는 유입이 남은 글까지 버리기 때문이었다. 그런데 격리를 지탱하던
두 근거(AdSense 재심사 리스크, 유입 손실)가 모두 사라졌고, 남겨 둔 272건이 사이트 정체성을
흐리는 쪽이 더 큰 비용이 됐다. 자세한 경위는 §7.2.

**레포를 public 으로 유지하는 이유:** GitHub Pages 무료 플랜은 public 레포에서만 사이트를
게시한다. private 으로 바꾸면 사이트가 내려간다. 반면 private 으로 얻는 보호는 없다 —
GA4 ID·AdSense 게시자 ID·Search Console 인증 토큰은 배포된 모든 페이지의 HTML 에 이미
들어 있고, `ads.txt` 는 공개 접근이 존재 이유다. 실제 비밀(`GROQ_API_KEY`,
`DATA_GO_KR_API_KEY`)은 GitHub Actions Secrets 에만 있으며 레포 공개 여부와 무관하게
노출되지 않는다. 워크플로우 트리거도 `schedule` 과 `workflow_dispatch` 뿐이라
포크 PR 로 시크릿이 새는 경로가 없다.

**중앙부처 우선으로 정한 이유:** 데이터 품질이 균일하고, 지자체는 건수가 많아 초기에
품질 관리가 어렵다. 이 결정이 단계 0의 작업량을 절반으로 줄인다 — API 4개가 아니라
**2개만 활용신청**하면 된다.

| API | 데이터셋 | 단계 0에서 신청 |
|-----|----------|-----------------|
| 행정안전부_대한민국 공공서비스(혜택) 정보 (보조금24) | `15113968` | ✅ |
| 한국사회보장정보원_중앙부처복지서비스 | `15090532` | ✅ |
| 한국사회보장정보원_지자체복지서비스 | `15108347` | 나중에 |
| 한국고용정보원_온통청년_청년정책 | `15143273` | 나중에 |

구현상 처리:

- `sync.py` 가 `REGION_SCOPE`(기본 `national`)로 발행 범위를 거른다. 범위 밖 제도는
  **버리는 게 아니라 발행만 보류**한다. 원천에서 매번 다시 읽으므로
  `REGION_SCOPE="national,sido,sigungu"` 로 바꾸면 그날 신규로 잡혀 그대로 발행된다.
- **이미 발행된 제도는 범위와 무관하게 계속 추적한다.** 범위를 좁혔다고 살아 있는
  페이지를 방치하면 정보가 낡은 채로 남는다.
- 지자체 제도가 없는 단계에서 시도 허브 17개를 만들면 빈 페이지만 늘어 색인에 해롭다.
  `_plugins/hub_generator.rb` 가 **제도가 실제로 있는 시도만** 허브를 만들고,
  `_layouts/hub.html` 의 탐색바도 같은 기준으로 링크를 건다(안 그러면 404).

### 남은 결정

| 항목 | 상태 |
|------|------|
| 세이프 브라우징 재심사 | Search Console 검토 요청 — 사용자 작업 |
| 자동 광고 복구 | 차단 해제 후 `ads_enabled: true` |
| 초기 백필(단계 7) | 발행 속도를 올릴 필요가 생기면 `PUBLISH_MODE=top` |
| 지자체까지 확대 | `REGION_SCOPE="national,sido,sigungu"`. 품질 편차를 감당할 준비가 된 뒤 |

---

## 부록: 파일 지도

실제 디렉터리 구조와 각 스크립트의 역할은 `docs/IMPLEMENTATION.md#파일-지도` 에 있다.
설계 시점의 안(案)은 몇 군데가 구현과 달라졌고(무엇이 왜 달라졌는지는
`docs/IMPLEMENTATION.md#초기-설계안에서-바꾼-것`), 지도가 둘이면 하나는 반드시 낡는다.
여기서는 하나만 둔다.
